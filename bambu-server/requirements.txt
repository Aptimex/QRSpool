# Custom library backend, for printers with "enable_custom_libraries": true.
# Not published to PyPI (yet), so these install straight from GitHub — which means pip
# needs git on PATH. They pull their own dependencies (paho-mqtt, cryptography).
bambu-mqtt-comms @ git+https://github.com/Aptimex/bambu-mqtt-comms.git@d40d387
bambu-mqtt-generator @ git+https://github.com/Aptimex/bambu-mqtt-generator.git@97de68b

# bambu.py backend, for printers with "enable_custom_libraries": false.
# Temporarily pointing to my fork of 2.6.6 while waiting for this bugfix merge: https://github.com/BambuTools/bambulabs_api/pull/180
bambulabs_api @ git+https://github.com/Aptimex/bambulabs_api.git@5296426

blinker==1.9.0
click==8.4.2
Flask==3.1.3
Flask-BasicAuth==0.2.0
flask-cors==6.0.5
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
paho-mqtt==2.1.0
pillow==12.3.0
Werkzeug==3.1.8
