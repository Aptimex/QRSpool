services:
  bambu-api-server:
    container_name: bambu-api-server
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
    build:
      context: .
    volumes:
      - .:/bambu-server
    ports:
      - "5123:5123"
